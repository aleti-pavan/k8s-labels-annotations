apiVersion: v1
kind: Pod
metadata:
   name: label-test2
   labels:
      env: testing
      owner: pavan
      team: sales
spec:
   containers:
      - name: kuard
        image: "gcr.io/kuar-demo/kuard-amd64:1"
        ports:
         - containerPort: 8080
           name: http
           protocol: TCP
            
  

